[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qlego-tket"
version = "0.1.0"
dependencies = [
    "qlego-core",
    "pytket",
    "pytket-qiskit"
]

[tool.setuptools.packages.find]
where = ["src"]
