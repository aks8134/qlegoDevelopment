[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qlego-qiskit"
version = "0.1.0"
dependencies = [
    "qlego-core",
    "qiskit",
    "qiskit-ibm-runtime"
]

[tool.setuptools.packages.find]
where = ["src"]
