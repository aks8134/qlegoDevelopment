[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qlego-cirq"
version = "0.1.0"
dependencies = [
    "qlego-core"
]

[tool.setuptools.packages.find]
where = ["src"]
